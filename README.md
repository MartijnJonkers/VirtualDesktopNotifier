# VirtualDesktopNotifier

uses [VirtualDesktopAccessor.dll](https://github.com/ciantic/virtualdesktopAccessor) to detect the current virtual desktop by index and the number of monitors.
This allows me to trigger a new minitor profile in display fusion as display fusion lacks the virtual desktop detection.
